"""消息路由"""

from fastapi import APIRouter

router = APIRouter()

# GET    /api/conversations/{id}/messages — 第 9 步实现
# POST   /api/conversations/{id}/messages — 第 9 步实现
# GET    /api/messages/{id}               — 第 9 步实现
# GET    /api/messages/search             — 第 9 步实现
# GET    /api/messages/{id}/detail        — 第 9 步实现
# DELETE /api/messages/{id}               — 第 9 步实现
# GET    /api/messages/{id}/contents      — 第 9 步实现
# GET    /api/messages/{id}/reasoning     — 第 9 步实现
# GET    /api/messages/{id}/tool-calls    — 第 9 步实现
# GET    /api/messages/{id}/metadata      — 第 9 步实现
