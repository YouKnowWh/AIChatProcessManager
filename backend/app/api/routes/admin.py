"""管理员路由"""

from fastapi import APIRouter

router = APIRouter()

# GET  /api/admin/users              — 第 12 步实现
# GET  /api/admin/users/{id}         — 第 12 步实现
# PUT  /api/admin/users/{id}         — 第 12 步实现
# DELETE /api/admin/users/{id}       — 第 12 步实现
# PUT  /api/admin/users/{id}/disable — 第 12 步实现
# PUT  /api/admin/users/{id}/enable  — 第 12 步实现
# GET  /api/admin/messages           — 第 12 步实现
# GET  /api/admin/messages/{id}/detail — 第 12 步实现
# PUT  /api/admin/messages/{id}/hide  — 第 12 步实现
# PUT  /api/admin/messages/{id}/restore — 第 12 步实现
# DELETE /api/admin/messages/{id}    — 第 12 步实现
# GET  /api/admin/feedbacks          — 第 12 步实现
# GET  /api/admin/stats              — 第 12 步实现
# POST /api/admin/audit-records      — 第 12 步实现
# GET  /api/admin/audit-records      — 第 12 步实现
